          echo What is in directory
          ls -a
          echo 
          echo is java installed
          java -version
          echo
          echo is git installed
          git --version
          echo
          echo what about build tools
          mvn --version
          gradle --version
          ant -version
          echo
          echo What environment variable  are avaible?
          env
